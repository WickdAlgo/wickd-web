/** Page-width container — the single source of horizontal rhythm. */
export const container = "mx-auto max-w-(--page-max-width) px-6";

/** Flat card shell used by platform panels. */
export const panel = "bg-card border border-hairline rounded-cards";
